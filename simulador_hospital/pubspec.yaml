name: hospital_simulator
description: Simulador de Hospital usando Discrete Event Simulation (DES)

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  fl_chart: ^0.66.0
  collection: ^1.18.0
  sqflite: ^2.4.2+1
  path: ^1.9.1
  path_provider: ^2.1.5
  syncfusion_flutter_datagrid: ^33.2.7
  syncfusion_flutter_core: ^33.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
